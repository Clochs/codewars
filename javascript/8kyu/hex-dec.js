// Complete the function which converts hex number (given as a string) to a decimal number.

// My solution

function hexToDec(hexString){
    // Convert str using parseInt and radix/base 16 (hex)
      return parseInt(hexString, 16);
    }